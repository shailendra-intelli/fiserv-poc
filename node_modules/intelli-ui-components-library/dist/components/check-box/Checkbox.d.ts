import React from "react";
export interface CheckboxProps {
    /** value props use for define the checkbox name */
    value?: string;
    /** color props used for define the color of the checkbox */
    color?: "primary" | "default" | "success" | "error" | "info" | "warning";
    /** size props use for define the size of the checkbox */
    size?: "sm" | "md" | "lg";
    /** poition props used for change the position of the checkbox with text */
    position?: "top" | "left" | "right" | "bottom";
    /** checked props used for define the checked or unchecked */
    checked?: boolean;
    /** disabled props used for define the disabled  checkbox with text name */
    disabled?: boolean;
    /** onChange props used for define the function like checked or unchecked */
    onChange?: (e: any) => void;
    /** indeterminate props used for define the three states a checkbox can be in: checked, unchecked, or indeterminate*/
    indeterminate?: boolean;
    /** defaultChecked props used for define by default selected checkbox*/
    defaultChecked?: boolean;
    /** circle props used for define to make a circle of checkbox */
    circle?: boolean;
}
/** Checkboxes allow the user to select one or more items from a set and also can be used to turn an option on or off. */
declare const Checkbox: React.FC<CheckboxProps>;
export default Checkbox;
