import React from "react";
export interface LoaderProps {
    /**width props used for size of the loader */
    width?: string;
    /**This variant can be used to render Loader of different types like Primary, success ,etc */
    variants?: string;
}
/**The Loader component is a visual indicator that expresses an indeterminate wait time */
declare const Loader: React.FC<LoaderProps>;
export default Loader;
