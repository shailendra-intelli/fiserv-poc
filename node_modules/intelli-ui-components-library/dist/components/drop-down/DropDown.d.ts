import React, { ReactNode } from "react";
type Option = {
    label: string;
    key: string;
};
export interface DropDownProps {
    /**optionList props is use for create a dropdown list for options*/
    optionList?: Option[];
    /**label props used to specify a label for an input element*/
    label?: string | boolean | ReactNode;
    /**onChange props used for event watches for changes in dropdwon field */
    onChange?: (val: any, value: any) => void;
    /** disabled props use for define the disabled dropdown */
    disabled?: boolean;
    /**The placeholder props places text inside the dropdown filed */
    placeholder?: string;
    /**The placeholderClass props used for create a custome class for placeholder  */
    placeholderClass?: string;
    /**The valueClass props used for create a custome class for value  */
    valueClass?: string;
    /**The entireFieldClickable props used for create entire field clickable */
    entireFieldClickable?: boolean;
    /**The error props used for create a error for dropdown field */
    error?: boolean;
    /**value props used to binding the data for an input element */
    value?: string | number;
    /**renderItem used for as a function and passed object and boolean */
    renderItem?: (obj: Option, bol: boolean) => ReactNode;
    /**renderSelected used for as a function render a selected item */
    renderSelected?: (obj: Option) => ReactNode;
    /** itemClass props used for define for a custome class for item */
    itemClass?: string;
    /** onToggle props used for toggle the dropdown like open and closed */
    onToggle?: (e: Event, state: boolean) => void;
    /** tabIndex props used for define an index of an item */
    tabIndex?: number;
    /**The dropdownClass props used for create a custome class for dropdown  */
    dropdownClass?: string;
    /**The activeItemClass props used for create a custome class for active item  */
    activeItemClass?: string;
    /** classNam props used for define for a custome class */
    className?: string;
    /**The labelClass props used for create a custome class for label  */
    labelClass?: string;
}
/**The dropdown component are used for collecting user provided information from a list of options. */
declare const DropDown: React.FC<DropDownProps>;
export default DropDown;
