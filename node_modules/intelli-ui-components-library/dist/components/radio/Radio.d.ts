import React from "react";
export interface RadioProps {
    /**style props used for create a cutome style */
    style?: any;
    /** bgColor props used for define the color of the radio */
    bgColor?: "primary" | "default" | "success" | "error" | "info" | "warning";
    /** onCloseClick props use for define to close */
    size?: "xs" | "sm" | "md";
    /** position props used for define the change of position with text name */
    position?: "top" | "left" | "right" | "bottom";
    /** value props used for define text of radio */
    value?: string;
    /** disable props used for define to disable the radio with text if disabled true */
    disabled?: boolean;
    /** className props used for define the custome class of radio  */
    className?: string;
    /**label props used to specify a label for an input element */
    label?: string;
    /** checked props used for define the checked or unchecked */
    checked?: boolean;
    /** id props used for define the unique id */
    id?: string;
}
/**The Radio Group allows the user to select one option from a set. */
declare const Radio: React.FC<RadioProps>;
export default Radio;
