import React, { ReactNode } from "react";
export interface ButtonGroupProps {
    /** round props used for define the shape of the buttonGroup */
    round?: "flat" | "round" | "pill";
    /** className props used for define the custome class for styling */
    className?: string;
    /** children props used for define that allows us to pass in any type of element */
    children?: ReactNode;
}
/**  The ButtonGroup component can be used to group related buttons */
declare const ButtonGroup: React.FC<ButtonGroupProps>;
export default ButtonGroup;
