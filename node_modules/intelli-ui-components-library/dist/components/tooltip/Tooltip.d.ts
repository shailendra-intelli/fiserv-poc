import React, { ReactNode } from "react";
export interface TooltipProps {
    /** message prop is used to show the tooltip message when select.  */
    message: string;
    /** position prop is used to set the position of the tooltip message.  */
    position: "top" | "bottom" | "left" | "right";
    /** children prop is used to show the name of the elements for the tooltip component.  */
    children: ReactNode;
    /** arrowIcon prop is used to set the arrow icon to each of the specific position's as per their alignment.  */
    arrowIcon?: boolean;
}
declare const Tooltip: React.FC<TooltipProps>;
export default Tooltip;
