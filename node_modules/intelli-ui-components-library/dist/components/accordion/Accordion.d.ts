import React, { ReactNode } from 'react';
export interface AccordionProps {
    /** Custom style for the accordion */
    style?: React.CSSProperties;
    /** Children elements within the accordion */
    children?: ReactNode;
    /** Custom class name for the accordion */
    className?: string;
}
export interface AccordionSectionProps {
    /** Custom style for the accordion section */
    style?: React.CSSProperties;
    /** The header of the accordion section */
    header: ReactNode;
    /** Custom class name for the accordion section */
    className?: string;
    /** Children elements within the accordion section */
    children?: ReactNode;
    /** Whether the accordion is expanded or not */
    expanded?: boolean;
    onOpenClick?: () => void;
    midChild?: ReactNode;
    rightChild?: ReactNode;
}
declare const Accordion: React.FC<AccordionProps> & {
    Section: React.FC<AccordionSectionProps>;
};
export default Accordion;
