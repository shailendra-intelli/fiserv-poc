import React, { ReactNode } from "react";
export interface FileUploadProps {
    /** onFileSelect function prop is used to add the functionality after the specific files has been selected.  */
    onFileSelect: (file: File) => void;
    /** acceptType prop is used to accept the format of files that needs to upload.  */
    fileType?: string;
    /** className prop is used to add the class to the FileUpload component.  */
    className?: string;
    /**label props used to specify a label for an input element */
    label?: string | ReactNode;
    /** fileInputClass prop is used to add the class to the FileUpload component.  */
    fileInputClass?: string;
    /** id to provide uniqueness to the FileUpload input element */
    id?: string;
}
declare const FileUpload: React.FC<FileUploadProps>;
export default FileUpload;
