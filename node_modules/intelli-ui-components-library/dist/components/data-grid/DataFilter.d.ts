import React from "react";
import { GridColDef } from "./DataGrid";
export type FilterFunction = "contains" | "startsWith" | "endsWith" | ">" | "<";
export interface GridFilterDef {
    logicalOperator: string;
    columnName: string;
    columnField: string;
    type: string;
    filterFunction: FilterFunction;
    value: string;
}
interface FilterProps {
    filters: GridFilterDef[];
    columns: GridColDef[];
    applyFilterHandler: (filtersArr: GridFilterDef[]) => void;
    closeFilterSelector: () => void;
}
declare const DataFilter: React.FC<FilterProps>;
export default DataFilter;
