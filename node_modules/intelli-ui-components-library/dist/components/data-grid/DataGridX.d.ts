import React, { ReactNode } from "react";
import { GridColDef } from "./DataGrid";
export interface DataGridXProps {
    rows: object[];
    columns: GridColDef[];
    headerColumns?: GridColDef[];
    /** className props used for define the custome class of Animatebutton */
    className?: string;
    /** children props used for define that allows us to pass in any type of element */
    children?: ReactNode;
    /**style props used for create a cutsome css for styling */
    style?: Record<string, unknown>;
    handleUpdatedRows?: (rowData: object[]) => void;
}
/** When pressed, a floating action button can display three to six related actions in the form of a Speed Dial. */
declare const DataGridX: React.FC<DataGridXProps>;
export default DataGridX;
export declare function convertJsonToCsv(headerData: any, jsonData: any, fileName: any): void;
