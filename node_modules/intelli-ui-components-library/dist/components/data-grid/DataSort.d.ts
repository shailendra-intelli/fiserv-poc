import React, { ReactNode } from "react";
import "./data-sort.scss";
export interface DataSortProps {
    sortKey: string;
    rowData: object[];
    handleSortedRowData: (rowData: any[]) => void;
    /** className props used for define the custome class of Animatebutton */
    className?: string;
    /** children props used for define that allows us to pass in any type of element */
    children?: ReactNode;
    /**style props used for create a cutsome css for styling */
    style?: Record<string, unknown>;
    iconAsc?: ReactNode;
    iconDesc?: ReactNode;
}
declare const DataSort: React.FC<DataSortProps>;
export default DataSort;
