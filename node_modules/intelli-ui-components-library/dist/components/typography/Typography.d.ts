import React from "react";
import "./typography.scss";
interface CustomStyle {
    property: string;
}
export interface TypographyProps {
    /** typoVariant props used for font weights and sizes in your application*/
    typoVariant?: "h1" | "h2" | "h3" | "h4" | "h5" | "h6" | "subheading1" | "body1" | "button" | "";
    /**typoColor is used to change the color of the typograhy */
    typoColor?: "primary" | "error" | "";
    /** children props use for define that allows us to pass in any type of element */
    children?: React.ReactNode;
    /**typoStyle is used for create a custome css for styling */
    typoStyle?: CustomStyle;
}
/**The Typography component makes it easy to apply a default set of font weights and sizes in your application. */
declare const Typography: React.FC<TypographyProps>;
export default Typography;
